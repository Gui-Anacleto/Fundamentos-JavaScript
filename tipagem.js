// Tipagem dinâmica
let minhaVAR = 123;
minhaVAr = "txt";
minhaVar = true;

// Multiparadigma

//Pode resolver problemas, de diversas formas.

//JavaScript não é Java

//o que é ES6 / ES2015 - versão marcante do JavaScript - trazendo funcionalidades para o dia dia(o que levou ao seu sucesso).

// JS - é uma linguagem interpretada : O codigo é executado na hora que estiver sendo rodado

// C,C++ - compiladas : Passa antes em um compilador, onde transforma em froma de maquina e passa para o PC,  depois executa em linguagem de maquina.

// NodeJS - backend - subir aplicações no backend (sem navegador)